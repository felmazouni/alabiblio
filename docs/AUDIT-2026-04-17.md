# Auditoria 2026-04-17

## Eliminar por completo

- Monorepo heredado de `apps/web`, `packages/*`, `scripts`, `sql`, `tests` y `docs`.
- Artefactos temporales, logs, previews y `node_modules`.
- Documentacion de roadmap anterior, contratos anteriores y auditorias anteriores.
- Componentes experimentales y decorativos sin valor para el rebuild.

## Salvar solo como conocimiento, no como base fisica

- Ideas de normalizacion y exclusion ya aplicadas a centros.
- Base conceptual del parser de horarios.
- Separacion previa entre dominio, contratos y worker.
- Aprendizajes sobre transporte, SER y encoding.

## Riesgos detectados en el repo heredado

- Mezcla de backlog correctivo con roadmap de producto.
- UI acoplada a contratos viejos y a exploracion previa.
- Demasiada superficie documental para una base que se va a rehacer.
- Tests congelando semanticas antiguas que no deben condicionar el nuevo modelo.

## Resultado de la limpieza ejecutada

- Se elimina el arbol operativo heredado.
- Se conserva el ZIP de `v0.app` en `references/v0/v0-export-2026-04-10.zip`.
- Se crea una base nueva orientada a Cloudflare desde cero.

